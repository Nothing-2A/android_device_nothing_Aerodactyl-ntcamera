hal_client_domain(platform_app, vendor_hal_nothing_camera)

allow platform_app dualcam_calibrate:dir create_dir_perms;
allow platform_app dualcam_calibrate:file create_file_perms;

get_prop(platform_app, audio_config_prop)
